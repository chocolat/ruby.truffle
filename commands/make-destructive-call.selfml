(trigger
    (key [[ctrl][shift]!]))

(name [Add ! to Method in Line / Selection])

(only-in source.ruby)

(input selection line)

(output snippet)

(save nothing)

(script [res=$(ruby "$TM_BUNDLE_SUPPORT/bin/make_destructive.rb")

if [ "$res" = "" ]
  then exit_show_tool_tip "Retry this command without a selection."
  else echo -n "$res"
fi])