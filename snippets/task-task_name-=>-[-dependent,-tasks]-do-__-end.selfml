(name [task :task_name => [:dependent, :tasks] do .. end])

(trigger
    (tab tas))

(only-in source.ruby)

(snippet [desc "%{1="Task description"}"
task :%{2="%{3="task_name"} => %{4="[:%{5="dependent, :tasks"}]"}"} do
	%{0}
end])